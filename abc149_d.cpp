#include <iostream>
#include <string>

using namespace std;

int main() {
    int N, K, R, S, P;
    string t;

    cin >> N >> K;
    cin >> R >> S >> P;
    cin >> t;

    cout << N + K + R + S + P << " " << t << endl;
}
